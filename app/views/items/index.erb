<div class='container'>
  <% @items.each do |item| %>
    <div class='panel panel-default'>
      <div class='panel-body'>
        <%= link_to item.title, item.url, target: :_blank %> - <i> published <%= time_ago_in_words(item.published_date) %> ago.</i>
      </div>
    </div>
  <% end %>
</div>
